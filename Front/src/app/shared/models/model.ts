export interface GeoLocation {
  latitude: number
  longitude: number
}

export interface Category {
  id: string;
  name: string;
  listing: number;
  logo: string;
  img: string;
}

export interface User {}
